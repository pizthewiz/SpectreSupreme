
NOW
[X] render view as a bitmap
[X] figure out why the output seems to lag sometimes
[X] support relative file URLs by looking for a string starting with './' or '../'?
[X] destination width and height
[~] render signal input
[ ] find zombie on -[SpectreSupreme _teardownWindow]
[ ] profile memory use with the render flag

LATER
[/] spin off rendering to a separate process
    not needed, the rendering happens off the main process as it is
[ ] offer source input
[ ] provide dynamic SVG creation example
[ ] provide dynamic canvas creation example
[ ] javascript execute input
[ ] GL direct pipeline?
